import React from "react";

function NewsListScreen({ onBack }) {
  return (
    <>
      <button onClick={onBack}>Back</button>
      <text>This is a test</text>
    </>
  );
}

export default NewsListScreen;
